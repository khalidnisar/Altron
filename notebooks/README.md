# Research notebooks

Start with `walk_forward_quickstart.ipynb`. Notebooks are examples only; reproducible production
runs should use the CLI and persist their exact input data, package lock, parameters, and seed.
